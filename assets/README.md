Place your app icon here as:

- `assets/line_tracker.ico`

If this file exists:

- source UI uses it for the window icon
- PyInstaller uses it for `LineTracker.exe` and `LineTrackerCli.exe`
- Inno Setup uses it for the installer icon

If the file does not exist, builds continue without a custom icon.
